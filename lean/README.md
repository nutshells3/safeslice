# Lean Sources

This folder carries the Lean problem files that motivated the detector core.

- `Problem1.lean`: witness-transition and detector-basis lower-bound side
- `Problem2.lean`: clarification-budget benchmark and restricted-family side

These files are included here so the computational implementation can be read
next to its formal source.
